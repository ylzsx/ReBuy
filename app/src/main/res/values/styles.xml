<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/grey</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="MyEditText" parent="Theme.AppCompat.Light">
        <item name="colorControlNormal">@color/black</item>
        <item name="colorControlActivated">@color/yellow</item>
    </style>

    <style name="SmallLabel">
        <item name="android:layout_weight">1</item>
        <item name="android:layout_margin">@dimen/margin_s1</item>
        <item name="android:padding">@dimen/padding_2</item>
        <item name="android:gravity">center</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:textSize">@dimen/size_s3</item>
        <item name="android:background">@drawable/shape_rectangle_with_radius</item>
    </style>
</resources>
